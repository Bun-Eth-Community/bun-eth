{
  "name": "@bun-eth/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "forge build",
    "deploy": "forge script script/Deploy.s.sol:DeployScript --rpc-url http://localhost:8545 --broadcast --legacy",
    "deploy:sepolia": "forge script script/Deploy.s.sol:DeployScript --rpc-url $SEPOLIA_RPC_URL --broadcast --verify",
    "test": "forge test -vv",
    "node": "anvil"
  }
}
